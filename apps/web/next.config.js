/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["res.cloudinary.com", "plus.unsplash.com", "sp-ao.shortpixel.ai", "png.pngtree.com"],
    },
};

export default nextConfig;
